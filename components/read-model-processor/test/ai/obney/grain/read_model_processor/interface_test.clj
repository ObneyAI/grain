(ns ai.obney.grain.read-model-processor.interface-test
  (:require [clojure.test :as test :refer :all]
            [ai.obney.grain.read-model-processor.interface :as read-model-processor]))

(deftest dummy-test
  (is (= 1 1)))

