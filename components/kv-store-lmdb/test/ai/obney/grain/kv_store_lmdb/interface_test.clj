(ns ai.obney.grain.kv-store-lmdb.interface-test
  (:require [clojure.test :as test :refer :all]
            [ai.obney.grain.kv-store-lmdb.interface :as kv-store-lmdb]))

(deftest dummy-test
  (is (= 1 1)))

